// writeLine(random().toString())

// writeLine(randomRange(20,50).toString())

// writeLine(string(random()))

// // This makes more sense
// loop(0, 10, writeLoop);
// function writeLoop(index) {
//     writeLine(index + "a");
// }

// loop(0, 10, function (index) {
//     writeLine(index + "a");
// });

// table('apples')
// row('color','name')
// row('red',"delicious")
// row('green','smith')

// writeLine()

// table('people')
// row('first name', 'last name', 'age')
// row('sally', 'smith', '23')
// row('david', 'jones', '40')

writeLine(random())
writeLine()
writeLine(random())
